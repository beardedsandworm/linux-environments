# Wormlogic routing baseline.
net.ipv4.ip_forward = 1

# Preserve Heighliner's known-good loose reverse-path filtering. This supports
# asymmetric/policy-routed traffic without disabling source validation entirely.
net.ipv4.conf.all.rp_filter = 2
net.ipv4.conf.default.rp_filter = 2
